create table ser_controle(
	id_ser smallint primary key,
	tipo char(1)
)
create table faccao(
	id_faccao smallint primary key,
	tipo char(50)
)
create table ser_controle(
	id_ser smallint primary key,
	tipo char(1)
)
create table ser_controle(
	id_ser smallint primary key,
	tipo char(1)
)
create table ser_controle(
	id_ser smallint primary key,
	tipo char(1)
);